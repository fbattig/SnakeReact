import React from 'react';
import Cell from './Cell';

const GRID_SIZE = 35;
const GRID = [];

for (let i = 0; i < GRID_SIZE; i++) {
  GRID.push(i);
}
const Row = ({y, key}) =>
<div className='grid-row'>
  
</div>

export default Row;